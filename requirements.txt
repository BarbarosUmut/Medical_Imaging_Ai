# Medical Imaging AI — Python dependencies
# torch and torchvision are excluded (install separately with CUDA support).
#
# Install: pip install -r requirements.txt

# Deep learning / medical imaging
monai[all]>=1.3.0

# Web UI
gradio>=4.0

# Image I/O and processing
nibabel>=5.0          # NIfTI (.nii / .nii.gz)
pydicom>=2.4          # DICOM
pillow>=10.0          # PIL — image loading and saving
scikit-image>=0.21    # CLAHE (skimage.exposure.equalize_adapthist)

# Numerical / scientific
numpy>=1.26
scipy>=1.11           # ndimage.zoom, binary_erosion, distance_transform_edt

# Machine learning metrics
scikit-learn>=1.3     # roc_auc_score, confusion_matrix, f1_score, …

# Visualisation
matplotlib>=3.8
seaborn>=0.13         # confusion-matrix heatmaps

# LLM / API
requests>=2.31        # Ollama REST API calls

# Testing
pytest>=7.0
